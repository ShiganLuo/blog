<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.baofeng.blog.mapper.UserMapper">
    
    <resultMap id="userResultMap" type="com.baofeng.blog.entity.User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="email" column="email"/>
        <result property="password" column="password"/>
        <result property="avatarUrl" column="avatar_url"/>
        <result property="bio" column="bio"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <result property="lastLogin" column="last_login"/>
        <result property="loginAttempts" column="login_attempts"/>
        <result property="isEmailVerified" column="is_email_verified"/>
        <result property="nickName" column="nick_name"/>
        <result property="phoneNumber" column="phone_number"/>
        <result property="gender" column="gender"/>
    </resultMap>

    <!-- 基础CRUD操作 -->
    <insert id="insertUser" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (
            username, email, password, avatar_url, bio, status,
            last_login, login_attempts, is_email_verified,nick_name,phone_number,gender
        ) VALUES (
            #{username}, #{email}, #{password}, #{avatarUrl}, #{bio}, 
            #{status}, #{lastLogin}, #{loginAttempts}, 
            #{isEmailVerified}, #{nickName}, #{phoneNumber}, #{gender}
        )
    </insert>

    <update id="updateUser">
        UPDATE users SET
            username = #{username},
            email = #{email},
            password = #{password},
            avatar_url = #{avatarUrl},
            bio = #{bio},
            status = #{status},
            last_login = #{lastLogin},
            login_attempts = #{loginAttempts},
            is_email_verified = #{isEmailVerified},
            nick_name = #{nickName},
            phone_number = #{phoneNumber},
            gender = #{gender}
        WHERE id = #{id}
    </update>

    <delete id="deleteUser">
        DELETE FROM users WHERE id = #{id}
    </delete>

    <!--查看指定时间时已存在的用户数-->
    <select id="selectUserCountWhenSpecifiedTime" resultType="java.lang.Long">
        SELECT COUNT(*) FROM users WHERE created_at &lt;= #{createdAt}
    </select>
    <!--根据用户名获取用户id-->
    <select id="getIdByUsername">
        select id FROM users WHERE username = #{username}
    </select>

    <select id="selectUserById" resultMap="userResultMap">
        SELECT * FROM users WHERE id = #{id}
    </select>

    <!-- 条件查询 -->
    <select id="selectByCondition" resultMap="userResultMap">
        SELECT * FROM users
        <where>
            <if test="username != null">
                AND username LIKE CONCAT('%', #{username}, '%')
            </if>
            <if test="email != null">
                AND email = #{email}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
    </select>

    <!-- 分页查询 -->
    <select id="selectByPage" resultMap="userResultMap">
        SELECT id, username, nick_name, email, phone_number, gender, avatar_url, status, last_login, created_at, updated_at FROM users
        ORDER BY id DESC
        LIMIT #{pageSize} OFFSET #{offset}
    </select>

    <!-- 登录相关操作 -->
    <select id="selectByUsernameOrEmail" resultMap="userResultMap">
        SELECT *
        FROM users
        WHERE username = #{account} OR email = #{account}
        LIMIT 1
    </select>

    <!--获取用户总数-->
    <select id="countAllUsers" resultType="Long">
        SELECT COUNT(*) FROM users
    </select>
    <update id="updateLoginInfo">
        UPDATE users SET
            last_login = NOW(),
            login_attempts = 0
        WHERE id = #{id}
    </update>

    <!--删除用户-->
    <delete id="deleteUserById">
        DELETE FROM users WHERE id = #{userId}
    </delete>

    <update id="incrementLoginAttempts">
        UPDATE users SET
            login_attempts = login_attempts + 1
        WHERE id = #{id}
    </update>
    <update id="updatePassword">
        UPDATE users
            SET password = #{password} 
        WHERE username = #{username}
    </update>

    <!--更新用户头像-->
    <update id="updateAvatarUrl">
        UPDATE users
            SET avatar_url = #{avatarUrl}, updated_at = NOW()
        WHERE id = #{id}
    </update>
    <!-- 动态更新 -->
    <update id="updateUserSelective">
        UPDATE users
        <set>
            <if test="username != null">username = #{username},</if>
            <if test="email != null">email = #{email},</if>
            <if test="password != null">password = #{password},</if>
            <if test="avatarUrl != null">avatar_url = #{avatarUrl},</if>
            <if test="bio != null">bio = #{bio},</if>
            <if test="status != null">status = #{status},</if>
            <if test="lastLogin != null">last_login = #{lastLogin},</if>
            <if test="loginAttempts != null">login_attempts = #{loginAttempts},</if>
            <if test="isEmailVerified != null">is_email_verified = #{isEmailVerified},</if>
            <if test="nickName != null">nick_name = #{nickName},</if>
            <if test="phoneNumber != null">phone_number = #{phoneNumber},</if>
            <if test="gender != null">gender = #{gender},</if> 
            updated_at = NOW()
        </set>
        WHERE id = #{id}
    </update>
</mapper> 