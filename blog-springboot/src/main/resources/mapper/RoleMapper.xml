<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.baofeng.blog.mapper.RoleMapper">
    <resultMap id="RoleResultMap" type="com.baofeng.blog.entity.Role">
        <id property="id" column="id"/>
        <result property="roleName" column="role_name"/>
        <result property="roleDesc" column="role_desc"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>
    
    <!-- 根据用户ID查询角色列表 -->
    <select id="selectRolesByUserId" resultMap="RoleResultMap">
        SELECT *
        FROM roles r
        INNER JOIN user_roles ur ON r.id = ur.role_id
        WHERE ur.user_id = #{userId}
    </select>

    <!-- 插入用户角色关联 -->
    <insert id="insertUserRole">
        INSERT INTO user_roles (user_id, role_id, created_at, updated_at)
        VALUES (#{userId}, #{roleId}, NOW(), NOW())
    </insert>
    <!-- 删除用户角色关联 -->
    <delete id="deleteUserRolesByUserId">
        DELETE FROM user_roles WHERE user_id = #{userId}
    </delete>
    <!--插入角色-->
    <insert id="insertRole" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO roles (role_name, role_desc, created_at, updated_at)
        VALUES (#{roleName}, #{roleDesc}, NOW(), NOW())
    </insert>
    <!--删除角色-->
    <delete id="deleteRoleById">
        DELETE FROM roles WHERE id = #{roleId}
    </delete>
    <!--判断角色是否存在-->
    <select id="selectRoleByRoleName">
        SELECT * FROM roles WHERE role_name = #{roleName}
    </select>

    <resultMap id="UserRoleResultMap" type="com.baofeng.blog.entity.UserRole">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="roleId" column="role_id"/>
        <result property="created_at" column="created_at"/>
        <result property="updated_at" column="updated_at"/>
    </resultMap>

</mapper>